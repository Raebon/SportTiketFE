import { FC } from 'react';
import { DepositComponent } from '../../components/balance/DepositComponent';

interface DepositProps {}

const Deposit: FC<DepositProps> = ({}) => {
  return <DepositComponent />;
};

export default Deposit;
