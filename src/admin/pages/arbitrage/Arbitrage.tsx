import { FC } from 'react';
import { ArbitrageFinder } from '../../components/arbitrage-finder/ArbitrageFinder';

interface ArbitrageProps {}

const Arbitrage: FC<ArbitrageProps> = ({}) => {
  return <ArbitrageFinder />;
};

export default Arbitrage;
